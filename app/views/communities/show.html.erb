<h2><%= @community.name %></h2>

<section class="admin-panel">
  <%= link_to "Add Category", new_community_category_path(@community) %>
</section>
<section class="categories">
  <%= render @community.categories %>
</section>
