<%= simple_form_for(post) do |f| %>
  <%= f.error_notification %>
  <%= f.hidden_field :topic_id %>
  <%= f.input :body, as: :text, label: 'Add Reply',
      input_html: {class: "tinymce"} %>
  <%= tinymce_assets %>
  <%= tinymce %>
  <%= f.submit "Submit" %>
<% end %>
